% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/1_e_helper.R
\name{select_training_data}
\alias{select_training_data}
\title{Helper: Select Training Data for Engine}
\usage{
select_training_data(norm_data, data)
}
\arguments{
\item{norm_data}{Logical flag indicating if normalized data should be used.}

\item{data}{A list with \code{original} and \code{normalized} elements.}
}
\value{
A data frame used for model training.
}
\description{
Retrieves the correct training dataset from the control object
based on whether normalization is enabled or not. This helper
is mandatory for all training engines within the flowengineR-framework.
}
\details{
\strong{Purpose:}
\itemize{
\item Ensures that each training engine uses the correct version of the data
(\code{original} vs. \code{normalized}) based on the normalization setting in the \code{control} object.
\item Prevents duplication of selection logic across multiple engines.
}

\strong{Expected Structure:}
\itemize{
\item \code{control$params$train$norm_data}: Logical flag indicating if normalized data should be used.
\item \code{control$params$train$data}: A list with \code{original} and \code{normalized} elements.
}

\strong{Usage Example (inside training engine):}

\if{html}{\out{<div class="sourceCode r">}}\preformatted{training_data <- select_training_data(control$params$train$norm_data, control$params$train$data)
model <- train(formula = control$params$train$formula, data = training_data, ...)
}\if{html}{\out{</div>}}
}
