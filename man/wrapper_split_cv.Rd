% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2_1_a__engine_split_cv__mwiller.R
\name{wrapper_split_cv}
\alias{wrapper_split_cv}
\title{Wrapper for Split Engine: Cross-Validation (CV)}
\usage{
wrapper_split_cv(control)
}
\arguments{
\item{control}{A standardized control object (see \code{controller_split()}).}
}
\value{
A standardized splitter output object with k-fold train/test splits.
}
\description{
Validates and prepares standardized inputs, merges default and user-defined hyperparameters,
and invokes the CV split engine. Returns standardized output using \code{initialize_output_split()}.
}
\details{
\strong{Standardized Inputs:}
\itemize{
\item \code{control$data$full}: Full dataset to be split (required).
\item \code{control$params$split$seed}: Integer seed for reproducibility.
\item \code{control$params$split$target_var}: Target variable for stratified splitting.
\item \code{control$params$split$params}: Optional engine-specific parameters.
}

\strong{Engine-Specific Parameters (\code{control$params$split$params}):}
This engine supports the following parameter:
\itemize{
\item \code{cv_folds} \emph{(integer, default = 5)}: Number of folds for cross-validation. Must be >1.
}

\strong{Notes:}
\itemize{
\item Stratification is applied based on the \code{target_var}.
\item If \code{cv_folds = 1}, validation fails because training would be empty.
}

\strong{Workflow Integration:}
\itemize{
\item \code{target_var} is \strong{automatically resolved} from \code{control$data$vars$target_var}
if not provided explicitly in the controller.
\item This allows users to define \code{target_var} only once in \code{controller_vars()}.
}

\strong{Example Control Snippet:}

\if{html}{\out{<div class="sourceCode">}}\preformatted{control$engine_select$split <- "split_cv"
control$params$split <- controller_split(
  seed = 42,
  target_var = "default",
  params = list(cv_folds = 5)
)
}\if{html}{\out{</div>}}

\strong{Template Reference:}
See full template in \verb{inst/templates_control/1_a_template_control_split_cv.R}

\strong{Standardized Output (returned to framework):}
A list structured via \code{initialize_output_split()} with:
\itemize{
\item \code{split_type}: "cv"
\item \code{splits}: List of named folds, each with \code{train} and \code{test} data.frames.
\item \code{seed}: Used seed.
\item \code{params}: Merged parameter list (user + default).
\item \code{specific_output}: Fold count and stratification info.
}
}
\seealso{
\code{\link[=engine_split_cv]{engine_split_cv()}},
\code{\link[=default_params_split_cv]{default_params_split_cv()}},
\code{\link[=initialize_output_split]{initialize_output_split()}},
\code{\link[=controller_split]{controller_split()}},
Template: \verb{inst/templates_control/1_a_template_control_split_cv.R}
Helper: \code{\link[=show_template]{show_template()}}
}
\keyword{internal}
