% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/1_d_controller.R
\name{controller_fairness_in}
\alias{controller_fairness_in}
\title{Controller: Fairness In-Processing Specification}
\usage{
controller_fairness_in(
  protected_attributes,
  target_var,
  norm_data = TRUE,
  params = list()
)
}
\arguments{
\item{protected_attributes}{Character vector. Protected attributes used during in-processing.}

\item{target_var}{Character string. Target variable of the predictive task.}

\item{norm_data}{Logical. Use normalized data (\code{TRUE}) or raw data (\code{FALSE}) in training.}

\item{params}{Named list. Engine-specific configuration. Default is empty list.}
}
\value{
Named list. To be stored in \code{control$params$fairness_in} and passed to the fairness in-processing engine. Compatible with all \code{fairnessToolbox} modules.
}
\description{
Generates a standardized input structure for any fairness in-processing engine
used within the fairnessToolbox workflow. This controller ensures compatibility
with built-in and custom in-processing methods that modify models during training.
}
\details{
Designed for use in the \code{control} object. In-processing methods can directly influence
the learning algorithm, e.g., by adjusting loss functions, applying reweighting, or
enforcing fairness constraints during optimization.

\strong{Purpose:}
\itemize{
\item Prepares metadata and parameters required for in-processing engines.
\item Supports optional normalization toggling at model level.
}

\strong{Standardized Structure:}
\itemize{
\item \code{protected_attributes}: Character vector of protected attribute names.
\item \code{target_var}: Character string. The outcome variable.
\item \code{norm_data}: Logical. Whether normalized data should be used for model training.
\item \code{params}: A named list of engine-specific parameters.
}

\strong{Usage Example:}

\if{html}{\out{<div class="sourceCode r">}}\preformatted{control$params$fairness_in <- controller_fairness_in(
  protected_attributes = c("gender"),
  target_var = "default",
  norm_data = TRUE,
  params = list(reweighting_method = "kamiran")
)
}\if{html}{\out{</div>}}
}
