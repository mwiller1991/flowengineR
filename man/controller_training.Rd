% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/1_d_controller.R
\name{controller_training}
\alias{controller_training}
\title{Controller: Training Engine Specification}
\usage{
controller_training(formula, norm_data = TRUE, params = list())
}
\arguments{
\item{formula}{A model formula used to define the structure of the predictive model.}

\item{norm_data}{Logical. Whether to normalize the input data (default: \code{TRUE}).}

\item{params}{Named list. Engine-specific configuration. Default is empty list.}
}
\value{
Named list. To be stored in \code{control$params$train} and passed to the training engine. Compatible with all \code{fairnessToolbox} modules.
}
\description{
Generates a standardized input structure for any training engine used
within the fairnessToolbox workflow. This includes the model formula,
optional hyperparameters, and a flag indicating whether to normalize input data.
}
\details{
Designed for use in the construction of the \code{control} object. Ensures compatibility
with all built-in and user-defined training engines and allows flexible extension.

\strong{Purpose:}
\itemize{
\item Specifies how the training model should be built and whether normalization is applied.
\item Enables tuning through engine-specific hyperparameters.
}

\strong{Standardized Structure:}
\itemize{
\item \code{formula}: A formula describing the model structure (e.g., \code{y ~ x1 + x2}).
\item \code{norm_data}: Logical. Whether the input data should be normalized before training.
\item \code{params}: A named list of hyperparameters passed to the training engine.
}

\strong{Usage Example:}

\if{html}{\out{<div class="sourceCode r">}}\preformatted{control$params$train <- controller_training(
  formula = default ~ age + income,
  norm_data = TRUE,
  params = list(n.trees = 100, interaction.depth = 3)
)
}\if{html}{\out{</div>}}
}
