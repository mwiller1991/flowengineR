% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2_1__initialize_output_split.R
\name{initialize_output_split}
\alias{initialize_output_split}
\title{Output Initializer: Splitter Engine Results}
\usage{
initialize_output_split(
  split_type,
  splits,
  seed,
  params = NULL,
  specific_output = NULL
)
}
\arguments{
\item{split_type}{Character. Short descriptor of the split strategy (e.g., \code{"random"}, \code{"cv"}).}

\item{splits}{Named list. Split definitions (e.g., list of train/test indices).}

\item{seed}{Integer. The random seed used for reproducibility.}

\item{params}{Optional. Named list of parameters used to configure the split.}

\item{specific_output}{Optional. Additional engine-specific metadata or diagnostic information.}
}
\value{
A named list containing standardized split output.
}
\description{
Creates a standardized output object for \strong{splitter engines} within the flowengineR
This initializer ensures that all splitter engines return their results in a uniform format,
making them compatible with downstream execution engines.
}
\details{
\strong{Purpose:}
\itemize{
\item Provides a consistent structure for data split definitions across all engines.
\item Enables flexible use of custom or built-in split strategies (e.g., random, stratified, CV).
}

\strong{Standardized Output:}
\itemize{
\item \code{split_type}: Character string describing the split method (e.g., \code{"random"}, \code{"cv"}).
\item \code{splits}: A named list of split definitions (e.g., data indices or data.frames).
\item \code{seed}: The random seed used for reproducibility.
\item \code{params}: Optional named list of parameters used by the splitter engine.
\item \code{specific_output}: Optional engine-specific metadata (e.g., fold summary, split ratios).
}

\strong{Usage Example (inside an engine):}

\if{html}{\out{<div class="sourceCode r">}}\preformatted{set.seed(control$split$seed)
n <- nrow(control$data)
idx <- sample(seq_len(n))
splits <- list("1" = list(train = idx[1:70], test = idx[71:100]))

initialize_output_split(
  split_type = "random",
  splits = splits,
  seed = control$split$seed,
  params = control$split$params
)
}\if{html}{\out{</div>}}
}
