% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/1_e_helper.R
\name{show_template}
\alias{show_template}
\title{Internal Helper: Show Control Template for an Engine}
\usage{
show_template(name, open = FALSE, return_content = FALSE)
}
\arguments{
\item{name}{Character. Engine identifier (e.g., \code{"split_random"} or \code{"train_lm"}).}

\item{open}{Logical. If \code{TRUE}, opens the template in RStudio (if available). Default is \code{FALSE}.}

\item{return_content}{Logical. If \code{TRUE}, returns the template content as character vector. Default is \code{FALSE}.}
}
\value{
Invisibly returns \code{TRUE} or, if \code{return_content = TRUE}, returns character vector. Errors if file not found.
}
\description{
Loads and optionally prints or opens a prewritten control template file from the
\verb{R/} directory of the \code{flowengineR} package using the naming convention
\verb{*_template_<engine>.R}.
}
\details{
\strong{Purpose:}
\itemize{
\item Provides users and developers with ready-to-use example code snippets for control object configuration.
}

\strong{Naming Convention:}
\itemize{
\item Template filenames must contain \verb{_template_<engine>.R} and be placed in the \verb{R/} folder of the package.
}

\strong{Usage Context:}
\itemize{
\item Intended for use during development or interactive prototyping.
}
}
\examples{
\dontrun{
show_template("split_random")           # Prints to console
show_template("split_random", open=TRUE)  # Opens in RStudio
text <- show_template("split_random", return_content = TRUE)  # Returns character vector
}
}
