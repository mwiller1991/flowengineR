% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/1_a_metalevel.R
\name{run_workflow_singlesplitloop}
\alias{run_workflow_singlesplitloop}
\title{Run a Single Workflow Iteration (One Train/Test Split)}
\usage{
run_workflow_singlesplitloop(control)
}
\arguments{
\item{control}{A fully specified control object for a single train/test split.}
}
\value{
A list with all outputs generated by the configured engines during this workflow iteration.
}
\description{
Executes a complete algorithmic workflow for a single split of training and test data.
Supports optional preprocessing, in-processing, post-processing, evaluation, and normalization steps,
all controlled by user-defined engine specifications.
}
\details{
This function is publicly accessible and is designed to be used within execution engines,
including sequential and parallel workflows (e.g., via batchtools or SLURM).

\strong{Inputs (from control object):}
\itemize{
\item \code{control$data$train}: Training data (data.frame).
\item \code{control$data$test}: Test data (data.frame).
\item \code{control$data$vars}: A list including:
\itemize{
\item \code{feature_vars}: Input variables used for training
\item \code{target_var}: Target variable
\item \code{protected_vars}: (Optional) Variables used for protected group analyses
\item \code{protected_vars_binary}: (Optional) Binary indicators for group-specific evaluation
}
\item \code{control$params$train}: Model training configuration.
\item \code{control$output_type}: Either \code{"response"} or \code{"prob"} (default: \code{"response"}).
\item Optional modules (if configured):
\itemize{
\item \code{control$preprocessing}: Name of preprocessing engine
\item \code{control$inprocessing}: Name of in-processing engine
\item \code{control$postprocessing}: Name of post-processing engine
\item \code{control$evaluation}: Vector of evaluation engine names
}
}

\strong{Output:}
A named list with standardized results:
\itemize{
\item \code{output_train}: Base model and predictions
\item \code{output_preprocessing}: (if used) Preprocessed training data
\item \code{output_inprocessing}: (if used) Adjusted model and predictions
\item \code{output_postprocessing}: (if used) Adjusted predictions
\item \code{output_eval}: (if used) Evaluation results
\item \code{normalization}: Parameters and method used for normalization (if applied)
}

\strong{Usage Notes:}
\itemize{
\item Input data must already be split before calling this function.
\item Normalization is based on training data and applied consistently to test data.
\item Engines must follow the standardized input/output format to be compatible.
\item This function is typically called internally by execution engines.
}
}
