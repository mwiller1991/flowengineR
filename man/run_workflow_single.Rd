% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/1_a_metalevel.R
\name{run_workflow_single}
\alias{run_workflow_single}
\title{Run a Single Iteration of the Fairness Workflow}
\usage{
run_workflow_single(control)
}
\arguments{
\item{control}{A fully configured control object with all necessary components.}
}
\value{
A list containing model results, predictions, fairness modules, and evaluations.
}
\description{
Executes a full modeling and fairness pipeline for a single train/test split.
Includes optional steps for fairness preprocessing, in-processing, post-processing,
as well as evaluation and normalization.
}
\details{
This function is \strong{publicly accessible} and is designed to be called in parallel workflows
(e.g., batchtools or SLURM). However, direct manual usage is discouraged in favor of
calling it through a dedicated execution engine, which ensures proper input preparation.

\strong{Inputs:}
\itemize{
\item \code{control$data$train}: Training data (data.frame).
\item \code{control$data$test}: Test data (data.frame).
\item \code{control$vars}: List with \code{feature_vars}, \code{target_var}, \code{protected_vars}, \code{protected_vars_binary}.
\item \code{control$params$train}: Model training configuration.
\item \code{control$output_type}: Either \code{"response"} or \code{"prob"} (default: \code{"response"}).
\item Optional modules:
\itemize{
\item \code{control$fairness_pre}: Preprocessing fairness engine name.
\item \code{control$fairness_in}: In-processing fairness engine name.
\item \code{control$fairness_post}: Post-processing fairness engine name.
\item \code{control$evaluation}: List of evaluation engine names.
}
}

\strong{Output:}
\itemize{
\item A named list with results from each stage:
\itemize{
\item \code{output_train}: Base model and predictions.
\item \code{output_fairness_pre}: (if applicable) Preprocessing output.
\item \code{output_fairness_in}: (if applicable) Adjusted in-processing model and predictions.
\item \code{output_fairness_post}: (if applicable) Adjusted post-processing predictions.
\item \code{output_eval}: (if applicable) Evaluation results per engine.
\item \code{normalization}: Used normalization parameters (if applied).
}
}

\strong{Usage Notes:}
\itemize{
\item \code{control$data$train} and \code{control$data$test} must already be defined.
\item Normalization is handled internally based on train data.
\item Designed for 1-split workflows. Use inside adaptive engines or SLURM jobs.
}
}
