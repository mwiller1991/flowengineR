% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/1_c__subregistry_validate_engines__mwiller.R
\name{validate_engine_split}
\alias{validate_engine_split}
\title{Subregistry Validation for Splitter Engines}
\usage{
validate_engine_split(wrapper_function, default_params_function, engine_name)
}
\arguments{
\item{wrapper_function}{Function. The wrapper function for the splitter engine.}

\item{default_params_function}{Function. Returns default parameters for the engine.}

\item{engine_name}{Character. Short name of the engine (e.g., \code{"split_random"}).}
}
\value{
TRUE if the engine is valid. Otherwise, an error is thrown.
}
\description{
Internally validates any splitter engine during registration via \code{register_engine()}.
Ensures that the wrapper adheres to expected structure, generates valid splits,
and uses the standardized \code{initialize_output_split()} format.
}
\details{
\strong{Purpose:}
\itemize{
\item Protects the framework from malformed or incomplete splitter engines.
\item Ensures interoperability across engines and workflow steps.
\item Enables automated integration of new splitter logic.
}

\strong{Validation Steps:}
\enumerate{
\item \strong{Structural Check}
Uses \code{validate_engine_structure()} to confirm correct wrapper signature and output initializer.
\item \strong{Functional Check}
Runs a dummy dataset through the engine to ensure:
\itemize{
\item At least one split is returned.
\item Each split includes both \code{train} and \code{test} components.
\item Split components are data frames.
}
}

\strong{Skipping Validation (Advanced Use):}
During development, wrappers may signal a skip by returning:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{list(skip_validation = TRUE)
}\if{html}{\out{</div>}}

\strong{Note:}
The engine \code{"split_userdefined"} is excluded from structural validation,
as it simply forwards externally defined splits.

\strong{Usage:}
Called automatically from \code{register_engine()} when registering \verb{split_*} engines.
}
\keyword{internal}
