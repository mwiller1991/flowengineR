% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2_1_c_engine_split_random.R
\name{wrapper_split_random}
\alias{wrapper_split_random}
\title{Wrapper for Split Engine: Random Split}
\usage{
wrapper_split_random(control)
}
\arguments{
\item{control}{A standardized control object (see \code{controller_split()}).}
}
\value{
A standardized splitter output object with train/test split.
}
\description{
Validates and prepares standardized inputs, merges default and user-defined hyperparameters,
and invokes the random split engine. Returns standardized output using \code{initialize_output_split()}.
}
\details{
\strong{Standardized Inputs:}
\itemize{
\item \code{control$data$full}: Full dataset to be split (required).
\item \code{control$params$split$seed}: Integer seed for reproducibility.
\item \code{control$params$split$target_var}: Target variable (not used in this engine, but required structurally).
\item \code{control$params$split$params}: Optional engine-specific parameters.
}

\strong{Engine-Specific Parameters (\code{control$params$split$params}):}
This engine supports the following parameter:
\itemize{
\item \code{split_ratio} \emph{(numeric, default = 0.7)}: Proportion of the dataset to use for training. Must be in (0, 1).
}

\strong{Notes:}
\itemize{
\item The target variable is ignored by this engine but must still be defined for compatibility.
\item This engine performs no stratification. For stratified splitting, consider using \code{"split_random_stratified"}.
}

\strong{Workflow Integration:}
\itemize{
\item \code{target_var} is \strong{automatically resolved} from \code{control$data$vars$target_var}
if not provided explicitly in the controller.
\item This allows users to define \code{target_var} only once in \code{controller_vars()}.
}

\strong{Example Control Snippet:}

\if{html}{\out{<div class="sourceCode">}}\preformatted{control$split_method <- "split_random"
control$params$split <- controller_split(
  seed = 42,
  target_var = "default",  # Required by framework, ignored here
  params = list(split_ratio = 0.6)
)
}\if{html}{\out{</div>}}

\strong{Template Reference:}
See full template in \verb{inst/templates/1_c_template_control_split_random.R}

\strong{Standardized Output (returned to framework):}
A list structured via \code{initialize_output_split()} with:
\itemize{
\item \code{split_type}: "random"
\item \code{splits}: Named list with one element \code{random} containing \code{train} and \code{test} data.frames.
\item \code{seed}: Used seed.
\item \code{params}: Merged parameter list (user + default).
\item \code{specific_output}: \code{NULL} (no additional metadata for this engine).
}
}
\seealso{
\code{\link[=engine_split_random]{engine_split_random()}},
\code{\link[=default_params_split_random]{default_params_split_random()}},
\code{\link[=initialize_output_split]{initialize_output_split()}},
\code{\link[=controller_split]{controller_split()}},
Template: \verb{inst/templates/1_c_template_control_split_random.R}
Helper: \code{\link[=show_template]{show_template()}}
}
\keyword{internal}
