% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/1_d_controller.R
\name{controller_preprocessing}
\alias{controller_preprocessing}
\title{Controller: Preprocessing Engine Configuration}
\usage{
controller_preprocessing(params = list())
}
\arguments{
\item{params}{Named list. Engine-specific configuration. Default is empty list.}
}
\value{
Named list. To be stored in \code{control$params$preprocessing} and passed to a preprocessing engine.
}
\description{
Generates a standardized input structure for any preprocessing engine
used within the \code{flowengineR} workflow. This includes steps like feature transformation,
resampling, reweighting, or preparation of group-specific attributes prior to model training.
}
\details{
Designed for use in the construction of the \code{control} object. Ensures compatibility
with all built-in and user-defined preprocessing engines.

\strong{Purpose:}
\itemize{
\item Supplies metadata and parameters for preprocessing steps before training.
\item Enables flexible customization of the data pipeline (e.g., stratification, normalization helpers, resampling strategies).
}

\strong{Automated Variable Handling:}
\itemize{
\item protected_attributes and target_var are \strong{automatically filled} from \code{control$data$vars}
\item This requires the use of \code{controller_vars()} to define the necessary references.
}

\strong{Standardized Structure:}
\itemize{
\item \code{params}: A named list of engine-specific configuration options.
}

\strong{Usage Example:}

\if{html}{\out{<div class="sourceCode r">}}\preformatted{control$params$preprocessing <- controller_preprocessing(
  params = list(method = "resample_by_group", group_var = "gender")
)
}\if{html}{\out{</div>}}
}
