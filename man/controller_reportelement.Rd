% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/1_d_controller.R
\name{controller_reportelement}
\alias{controller_reportelement}
\title{Controller: Reportelement Input Specification}
\usage{
controller_reportelement(params = NULL)
}
\arguments{
\item{params}{Optional named list. Each name corresponds to a reportelement alias, and each value is a list of engine-specific parameters.}
}
\value{
A named list to be stored in \code{control$params$reportelement}, compatible with all reportelement engines.
}
\description{
Generates a standardized input structure for reportelement engines in the fairnessToolbox
workflow. Each element is independently configurable and mapped to a reporting alias.
}
\details{
Designed for modular and extensible usage within the \code{control} object. Each alias allows
for distinct visualizations, tables, or summaries, depending on the reporting engine used.

\strong{Purpose:}
\itemize{
\item Enables flexible configuration of multiple reportelements.
\item Supports alias-based referencing to match user-specified reporting tasks.
}

\strong{Standardized Structure:}
\itemize{
\item \code{params}: A named list of parameter lists. Each name must correspond to an alias specified
in \code{control$reportelement}, which in turn maps to a specific reportelement engine.
}

\strong{Usage Example:}

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Alias definitions (e.g. assigned engines)
control$reportelement <- list(
  alias1 = "reportelement_table_splitmetrics",
  alias2 = "reportelement_boxplot_predictions"
)

# Corresponding alias-specific parameter setup
control$params$reportelement <- controller_reportelement(
  params = list(
    alias1 = list(format = "wide"),
    alias2 = list(show_points = TRUE)
  )
)
}\if{html}{\out{</div>}}
}
