% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/1_e__helper_mwiller.R
\name{complete_control_with_defaults}
\alias{complete_control_with_defaults}
\title{Internal Helper: Complete Control Object with Defaults}
\usage{
complete_control_with_defaults(control)
}
\arguments{
\item{control}{list. A (partially defined) user control object.}
}
\value{
list. A fully structured control object with completed defaults.
}
\description{
Fills a partially specified \code{control} object with default values for all
mandatory components up to the evaluation stage. Optional elements such as
processing engines, reporting, or publishing are only initialized if explicitly
selected by the user.
}
\details{
\strong{Purpose:}
\itemize{
\item Ensures a minimal, runnable workflow configuration even with partial user input.
\item Enables prototyping or quick-start testing without full manual specification.
}

\strong{Default Behavior:}
\itemize{
\item Sets \code{global_seed} to \code{1} if missing.
\item Uses \code{flowengineR::test_data_2_base_credit_example} as fallback data if neither
\code{control$data$vars} nor \code{control$data$full} are provided.
\item Initializes \code{data$train} and \code{data$test} to \code{NULL} if missing.
\item Sets \code{split = "split_random_stratified"} and calls \code{controller_split()} if missing.
\item Sets \code{execution = "execution_basic_sequential"} and calls \code{controller_execution()} if missing.
\item Sets \code{train = "train_glm"} and \code{output_type = "response"} if missing.
\item Calls \code{controller_training()} if \code{params$train} is missing.
\item Adds default evaluation methods if not specified: \code{"eval_mse"}, \code{"eval_summarystats"}, \code{"eval_statisticalparity"}.
\item Calls \code{controller_evaluation()} if \code{params$eval} is missing.
\item For optional modules (\code{preprocessing}, \code{inprocessing}, \code{postprocessing}, \code{reportelement}, \code{report}, \code{publish}),
corresponding controller functions are only called if the module is selected by the user.
}

\strong{Safety Checks:}
\itemize{
\item If \code{control$data$full} is provided without \code{control$data$vars}, an error is raised.
\item If \code{control$data$vars} is provided without \code{control$data$full}, an error is raised.
}

\strong{Usage Example:}

\if{html}{\out{<div class="sourceCode r">}}\preformatted{control <- list(train = "train_glm")
control <- complete_control_with_defaults(control)
# returns a runnable control object with defaults and dummy data
}\if{html}{\out{</div>}}
}
\keyword{internal}
