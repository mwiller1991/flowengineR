% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/1_d_controller.R
\name{controller_publish}
\alias{controller_publish}
\title{Controller: Publish Input Specification}
\usage{
controller_publish(output_folder = NULL, params = list())
}
\arguments{
\item{output_folder}{Character string. Global directory path to store all exports.}

\item{params}{Named list. Engine-specific configuration. Default is empty list. Each name should match a \code{control$publish} alias, and contain a list of export parameters (\code{obj_type}, \code{obj_name}, etc.).}
}
\value{
Named list. To be stored in \code{control$params$publish} and passed to the publishing engine(s). Compatible with all \code{fairnessToolbox} modules.
}
\description{
Generates a standardized input structure for publishing engines in the fairnessToolbox
workflow. This includes both a global output folder and per-alias settings for how
specific content (e.g., reports or reportelements) should be exported.
}
\details{
Designed to support flexible export of results, including batch publishing of various
report formats (e.g., HTML, PDF, JSON) or custom targets (e.g., web endpoints, folders).

\strong{Purpose:}
\itemize{
\item Enables modular export pipelines for reporting results.
\item Decouples report/reportelement definitions from export configuration.
}

\strong{Standardized Structure:}
\itemize{
\item \code{output_folder}: Root directory for all published files.
\item \code{params}: A named list of configuration lists, each matching an alias in \code{control$publish}.
}

\strong{Usage Example:}

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Alias definitions (mapped to export engines)
control$publish <- list(
  html_summary = "publish_html",
  full_json = "publish_json"
)

# Corresponding publishing configuration
control$params$publish <- controller_publish(
  output_folder = "results/exports/",
  params = list(
    html_summary = list(obj_type = "report", obj_name = "alias1"),
    full_json = list(obj_type = "reportelement", obj_name = "alias2")
  )
)
}\if{html}{\out{</div>}}
}
