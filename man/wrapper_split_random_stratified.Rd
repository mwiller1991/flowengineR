% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2_1_b_engine_split_random_stratified.R
\name{wrapper_split_random_stratified}
\alias{wrapper_split_random_stratified}
\title{Wrapper for Split Engine: Stratified Random Split}
\usage{
wrapper_split_random_stratified(control)
}
\arguments{
\item{control}{A standardized control object (see \code{controller_split()}).}
}
\value{
A standardized splitter output object with stratified train/test split.
}
\description{
Validates and prepares standardized inputs, merges default and user-defined hyperparameters,
and invokes the stratified random split engine. Returns standardized output using \code{initialize_output_split()}.
}
\details{
\strong{Standardized Inputs:}
\itemize{
\item \code{control$data$full}: Full dataset to be split (required).
\item \code{control$params$split$seed}: Integer seed for reproducibility.
\item \code{control$params$split$target_var}: Target variable used for stratification.
\item \code{control$params$split$params}: Optional engine-specific parameters.
}

\strong{Engine-Specific Parameters (\code{control$params$split$params}):}
This engine supports the following parameter:
\itemize{
\item \code{split_ratio} \emph{(numeric, default = 0.7)}: Proportion of the dataset to use for training. Must be in (0, 1).
}

\strong{Notes:}
\itemize{
\item The stratification is performed using \code{caret::createDataPartition()} and ensures balanced class representation in train/test sets.
\item If the target variable has rare categories, consider whether stratification is meaningful.
}

\strong{Workflow Integration:}
\itemize{
\item \code{target_var} is \strong{automatically resolved} from \code{control$data$vars$target_var}
if not provided explicitly in the controller.
\item This allows users to define \code{target_var} only once in \code{controller_vars()}.
}

\strong{Example Control Snippet:}

\if{html}{\out{<div class="sourceCode">}}\preformatted{control$split_method <- "split_random_stratified"
control$params$split <- controller_split(
  seed = 123,
  target_var = "default",
  params = list(split_ratio = 0.75)
)
}\if{html}{\out{</div>}}

\strong{Template Reference:}
See full template in \verb{inst/templates_control/1_b_template_control_split_random_stratified.R}

\strong{Standardized Output (returned to framework):}
A list structured via \code{initialize_output_split()} with:
\itemize{
\item \code{split_type}: "random_stratified"
\item \code{splits}: Named list with one element \code{random_stratified} containing \code{train} and \code{test} data.frames.
\item \code{seed}: Used seed.
\item \code{params}: Merged parameter list (user + default).
\item \code{specific_output}: \code{NULL} (no additional metadata for this engine).
}
}
\seealso{
\code{\link[=engine_split_random_stratified]{engine_split_random_stratified()}},
\code{\link[=default_params_split_random_stratified]{default_params_split_random_stratified()}},
\code{\link[=initialize_output_split]{initialize_output_split()}},
\code{\link[=controller_split]{controller_split()}},
Template: \verb{inst/templates_control/1_b_template_control_split_random_stratified.R}
Helper: \code{\link[=show_template]{show_template()}}
}
\keyword{internal}
