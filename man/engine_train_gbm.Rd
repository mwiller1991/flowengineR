% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2_4_c__engine_train_gbm__mwiller.R
\name{engine_train_gbm}
\alias{engine_train_gbm}
\title{Training Engine: Gradient Boosting Machine (GBM)}
\usage{
engine_train_gbm(
  formula,
  data,
  sample_weight,
  distribution,
  n.trees,
  interaction.depth,
  shrinkage,
  n.minobsinnode,
  bag.fraction,
  train.fraction
)
}
\arguments{
\item{formula}{Model formula.}

\item{data}{Training data as data.frame.}

\item{sample_weight}{Numeric vector of observation weights.}

\item{distribution}{GBM loss distribution.}

\item{n.trees}{Number of boosting iterations.}

\item{interaction.depth}{Tree depth.}

\item{shrinkage}{Learning rate.}

\item{n.minobsinnode}{Minimum obs per terminal node.}

\item{bag.fraction}{Subsample fraction per tree.}

\item{train.fraction}{Fraction of data used for training.}
}
\value{
A fitted model object of class \code{"gbm"}.
}
\description{
Fits a GBM model based on the provided formula, data, and weights using the \code{gbm} package.
}
\details{
\strong{Inputs (passed to engine via wrapper):}
\itemize{
\item \code{formula}: A formula specifying the model structure.
\item \code{data}: A data frame containing the training data (must include weights column if used).
\item \code{sample_weight}: A numeric vector of observation weights.
\item \code{distribution}: Loss distribution (e.g., "gaussian", "bernoulli").
\item \code{n.trees}: Number of boosting iterations (trees).
\item \code{interaction.depth}: Depth of each tree.
\item \code{shrinkage}: Learning rate.
\item \code{n.minobsinnode}: Minimum observations in terminal nodes.
\item \code{bag.fraction}: Subsampling fraction for stochastic gradient boosting.
\item \code{train.fraction}: Fraction of data used for training (rest for internal OOB-like eval).
}

\strong{Output (returned to wrapper):}
\itemize{
\item A fitted model object of class \code{"gbm"} as returned by \code{gbm::gbm()}.
}
}
\seealso{
\code{\link[=wrapper_train_gbm]{wrapper_train_gbm()}}
}
\keyword{internal}
