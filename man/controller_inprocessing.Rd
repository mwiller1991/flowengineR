% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/1_d_controller.R
\name{controller_inprocessing}
\alias{controller_inprocessing}
\title{Controller: In-Processing Engine Configuration}
\usage{
controller_inprocessing(norm_data = TRUE, params = list())
}
\arguments{
\item{norm_data}{Logical. Whether to use normalized data (default: TRUE).}

\item{params}{Named list. Engine-specific configuration. Default is empty list.}
}
\value{
Named list. To be stored in \code{control$params$inprocessing} and passed to an in-processing engine.
}
\description{
Generates a standardized input structure for any in-processing engine
used within the \code{flowengineR} workflow. In-processing engines modify the model training process directly,
for example by applying constraints, reweighting, or custom optimization procedures.
}
\details{
Designed for use in the construction of the \code{control} object. Ensures compatibility
with built-in and user-defined in-processing strategies.

\strong{Purpose:}
\itemize{
\item Passes relevant metadata and hyperparameters to in-processing methods.
\item Enables direct influence on the model training logic (e.g., bias penalties, group-specific loss weights).
}

\strong{Automated Variable Handling:}
\itemize{
\item protected_attributes and target_var are \strong{automatically derived} from \code{control$data$vars}
if not manually specified.
\item This requires prior definition via \code{controller_vars()}.
}

\strong{Standardized Structure:}
\itemize{
\item \code{norm_data}: Logical. Whether to use normalized data (default: \code{TRUE}).
\item \code{params}: A named list of engine-specific hyperparameters.
}

\strong{Usage Example:}

\if{html}{\out{<div class="sourceCode r">}}\preformatted{control$params$inprocessing <- controller_inprocessing(
  norm_data = TRUE,
  params = list(constraint = "equalized odds", lambda = 0.1)
)
}\if{html}{\out{</div>}}
}
