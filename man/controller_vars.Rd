% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/1_d__controller__mwiller.R
\name{controller_vars}
\alias{controller_vars}
\title{Controller: Variable Definition Specification}
\usage{
controller_vars(
  feature_vars,
  protected_vars,
  target_var,
  protected_vars_binary
)
}
\arguments{
\item{feature_vars}{Character vector. Input features used to train the predictive model.}

\item{protected_vars}{Character vector. Protected attributes used in fairness preprocessing or grouping.}

\item{target_var}{Character. Name of the target variable (dependent variable).}

\item{protected_vars_binary}{Character vector. Protected variables prepared for fairness evaluation (e.g., binary dummies).}
}
\value{
Named list. To be stored in \code{control$data$vars} and passed to all engines that require variable references. Compatible with all \code{flowengineR} modules.
}
\description{
Creates a standardized structure containing all variable names required across
the flowengineR workflow, including input features, protected attributes, target variable,
and optionally grouped or binarized versions of protected attributes for evaluation.
}
\details{
Designed for use in the construction of the \code{control} object. This controller ensures
that variable references are consistently named and available for engines handling
training, fairness adjustments, and evaluation.

\strong{Purpose:}
\itemize{
\item Centralizes all variable references for modular engines.
\item Ensures compatibility with pre-, in-, and post-processing fairness engines as well as evaluation components.
}

\strong{Standardized Structure:}
\itemize{
\item \code{feature_vars}: Character vector of input features.
\item \code{protected_vars}: Character vector of protected attributes (e.g., gender, age_group).
\item \code{target_var}: Name of the target variable to be predicted.
\item \code{protected_vars_binary}: Character vector of grouped or binary-coded protected attributes used in evaluation.
}

\strong{Usage Example:}

\if{html}{\out{<div class="sourceCode r">}}\preformatted{control$data$vars <- controller_vars(
  feature_vars = c("income", "loan_amount"),
  protected_vars = c("gender", "marital_status"),
  target_var = "default",
  protected_vars_binary = c("gender_Female", "marital_status_Married")
)
}\if{html}{\out{</div>}}
}
