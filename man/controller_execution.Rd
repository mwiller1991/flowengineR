% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/1_d_controller.R
\name{controller_execution}
\alias{controller_execution}
\title{Controller: Execution Engine Specification}
\usage{
controller_execution(params = list())
}
\arguments{
\item{params}{Named list. Engine-specific configuration. Default is empty list.}
}
\value{
Named list. To be stored in \code{control$params$execution} and passed to the execution engine. Compatible with all \code{fairnessToolbox} modules.
}
\description{
Generates a standardized input structure for configuring any execution engine
used within the fairnessToolbox workflow. This controller supports both sequential
and parallel/adaptive execution strategies, including those relying on external resources
like SLURM or batchtools.
}
\details{
Designed for use in the construction of the \code{control} object. The structure is compatible
with all built-in and user-defined execution engines and ensures extensibility.

\strong{Purpose:}
\itemize{
\item Encapsulates all parameters needed for executing the training pipeline.
\item Supports customization for parallelization, stability checks, and output persistence.
}

\strong{Standardized Structure:}
\itemize{
\item \code{params}: A named list of engine-specific parameters (e.g., \code{ncpus}, \code{output_folder}, \code{max_splits}).
}

\strong{Usage Example:}

\if{html}{\out{<div class="sourceCode r">}}\preformatted{control$params$execution <- controller_execution(
  params = list(
    output_folder = "results/batch/",
    max_splits = 30,
    ncpus = 4
  )
)
}\if{html}{\out{</div>}}
}
