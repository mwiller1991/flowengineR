% Generated by roxygen2: do not edit by hand
% Please edit documentation in
%   R/2_2__zzz_helper_execution_stability_checks__mwiller.R
\name{check_stability_cohen_absolute}
\alias{check_stability_cohen_absolute}
\title{Internal Stability Strategy: Cohen’s d (Absolute Difference)}
\usage{
check_stability_cohen_absolute(values, threshold, window, fun = NULL)
}
\arguments{
\item{values}{Numeric vector. Metric values collected across iterations.}

\item{threshold}{Numeric. Maximum allowed Cohen’s d effect size.}

\item{window}{Integer. Number of trailing values to include in the window.}

\item{fun}{Ignored. Included for consistent interface across strategies.}
}
\value{
A named list:
\itemize{
\item \code{is_stable}: Logical. Indicates if stability is achieved.
\item \code{stability_value}: Computed Cohen’s d effect size.
\item \code{threshold_value}: The threshold provided as input.
\item \code{strategy}: Fixed string \code{"cohen_absolute"}.
}
}
\description{
Evaluates the effect size between the most recent metric values (\code{window}) and
the earlier observations using \strong{Cohen’s d}. Stability is considered achieved if
the effect size is smaller than the defined threshold.
}
\details{
\strong{Stability Rule:}
\itemize{
\item Compute mean and standard deviation for both:
\itemize{
\item \code{window}: Last \code{window} values.
\item \code{global}: All preceding values.
}
\item Compute pooled standard deviation:
\deqn{sd_pooled = sqrt((sd_1^2 + sd_2^2) / 2)}
\item Compute effect size:
\deqn{d = |mean_1 - mean_2| / sd_pooled}
\item If \code{d < threshold}, then \code{is_stable = TRUE}.
}

\strong{Use Case:}
\itemize{
\item Appropriate when magnitude of mean difference relative to variability matters.
}

\strong{Notes:}
\itemize{
\item Falls back to \code{Inf} if not enough global values are available.
\item \code{fun} argument is ignored; included for interface consistency.
}
}
\keyword{internal}
